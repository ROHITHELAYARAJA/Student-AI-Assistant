require('../backend/dist/config');
(async()=>{const region=process.env.AWS_REGION;const id='amazon.nova-lite-v1:0';const r=await fetch(`https://bedrock-runtime.${region}.amazonaws.com/model/${encodeURIComponent(id)}/converse`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${process.env.AWS_BEARER_TOKEN_BEDROCK}`},body:JSON.stringify({messages:[{role:'user',content:[{text:'Reply with READY.'}]}],inferenceConfig:{maxTokens:20,temperature:0}}),signal:AbortSignal.timeout(25000)});const data=await r.json();console.log(JSON.stringify({model:id,status:r.status,errorType:r.headers.get('x-amzn-errortype'),message:data.message,output:data.output,usage:data.usage}));})();
