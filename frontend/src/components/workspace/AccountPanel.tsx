import React,{useState} from 'react';
import {request} from '../../services/studyApi';
export function AccountPanel({session,onChange}:{session:any;onChange:()=>void}){
 const [mode,setMode]=useState<'login'|'register'>('register'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await request(`/auth/${mode}`,{method:'POST',body:JSON.stringify({email,password,name:name||'Student'})});setPassword('');onChange();}catch(e:any){setError(e.message);}finally{setBusy(false);}}
 if(session?.user?.email)return <div className="account-panel"><strong>{session.user.name}</strong><p>Signed in as {session.user.email}. Your notebooks are saved on the study server.</p><button className="subtle-button" onClick={async()=>{try{await request('/auth/logout',{method:'POST'});onChange();}catch(e:any){setError(e.message);}}}>Sign out</button>{error&&<p role="alert">{error}</p>}</div>;
 return <form className="account-panel" onSubmit={submit}><h3>{mode==='register'?'Save your learning across sessions':'Welcome back'}</h3><p>{session?.guest?'You are using a private guest workspace. Register to keep access to it.':'Sign in to access your notebooks.'}</p>{mode==='register'&&<><label className="field-label" htmlFor="account-name">Name</label><input id="account-name" value={name} onChange={e=>setName(e.target.value)} autoComplete="name" required maxLength={50}/></>}<label className="field-label" htmlFor="account-email">Email</label><input id="account-email" value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/><label className="field-label" htmlFor="account-password">Password · at least 10 characters</label><input id="account-password" value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete={mode==='register'?'new-password':'current-password'} minLength={10} maxLength={128} required/>{error&&<p className="inline-error" role="alert">{error}</p>}<button className="dark-button modal-submit" disabled={busy}>{busy?'One moment…':mode==='register'?'Create account':'Sign in'}</button><button type="button" className="text-button" onClick={()=>{setMode(mode==='register'?'login':'register');setError('');}}>{mode==='register'?'Already registered? Sign in':'New here? Create an account'}</button></form>;
}
